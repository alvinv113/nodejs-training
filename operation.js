const add = (a,b) => console.log(Number(a)+Number(b));
const sub = (a,b) => console.log(Number(a)-Number(b));
const mul = (a,b) => console.log(Number(a)*Number(b));
const div = (a,b) => console.log(Number(a)/Number(b));

module.exports = {
  add,
  sub,
  mul,
  div,
};
